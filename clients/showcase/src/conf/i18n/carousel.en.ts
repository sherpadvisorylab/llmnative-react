import { defineLocaleMessages } from '@llmnative/react';

export default defineLocaleMessages({
    showcase: {
        carousel: {
            page: { title: 'Carousel', description: 'Image carousel wrapper with indicators, controls and caption support.' },
            sections: {
                slides: { title: 'Slides with captions' },
            },
        },
    },
});
