import { defineLocaleMessages } from '@llmnative/react';

export default defineLocaleMessages({
    showcase: {
        grid: {
            page: {
                title: 'Grid',
                description: 'AI-first orchestration layer built on top of Table and Gallery. The page now moves from the smallest valid grid to progressively more explicit data, selection, action and layout patterns.',
            },
            labels: {
                copy: 'Copy',
                copied: 'Copied!',
                name: 'Name',
                email: 'Email',
                role: 'Role',
                status: 'Status',
                team: 'Team',
                city: 'City',
                contact: 'Contact',
                fullName: 'Full name',
                activeKey: 'Active key',
                none: 'none',
                selectedCount: '{count} selected',
                currentOrder: 'Current order',
            },
            values: {
                roles: { admin: 'Admin', editor: 'Editor', viewer: 'Viewer' },
                statuses: { active: 'Active', review: 'Review', inactive: 'Inactive' },
            },
            examples: {
                sources: {
                    title: 'Grid data sources',
                    description: 'This section shows where Grid records come from. Start from the shortest provider-backed shape, then add route-based sourcing, query constraints, or switch to caller-owned records when you need more control.',
                    items: {
                        minimal: { tab: 'Minimal', title: 'Load data from an explicit path', description: 'The shortest valid grid. The gateway component subscribes to a provider path and infers columns from the incoming records.' },
                        pathQuery: { tab: 'Path + query', title: 'GridDB with filter + ordering', description: 'The same provider-backed load, now reduced to active teammates and reordered by email ascending. Grid still infers the columns from the incoming records.' },
                        array: { tab: 'Array', title: 'GridArray with caller-owned records', description: 'Use GridArray when the caller already owns the dataset and Grid only needs to render it.' },
                        fromUrl: { tab: 'fromUrl', title: 'Load data from the current URL', description: 'Use fromUrl when the current route already matches the provider path you want to read. Grid forwards the pathname as-is, without query string or hash.' },
                    },
                },
                columns: {
                    title: 'Columns',
                    description: 'Columns are the second decision. You can let Grid infer them, declare them explicitly, or add custom renderers when one field needs a visual treatment.',
                    items: {
                        infer: { tab: 'Infer', title: 'No columns prop', description: 'Useful for the shortest CRUD paths: Grid infers columns from the records or the form.' },
                        explicit: { tab: 'Explicit', title: 'Explicit labels and sortable fields', description: 'Use this when you want the schema to be visible and predictable.' },
                        render: { tab: 'Render', title: 'Custom cell renderer', description: 'When one column needs visual emphasis, keep the rest declarative and customize only that field.' },
                    },
                },
                sorting: {
                    title: 'Sorting',
                    description: 'Sorting can stay fully enabled, be disabled entirely, or be limited to just the fields that benefit from it.',
                    items: {
                        off: { tab: 'Off', title: 'No sorting', description: 'Disable sorting when the order is fixed or when the grid is only meant to present records.' },
                        someFields: { tab: 'Some fields', title: 'Sorting only where it helps', description: 'Keep sorting on the fields users actually compare, and leave the others static.' },
                        default: { tab: 'Default', title: 'Classic sortable grid', description: 'This is the default behavior: sortable columns stay active and Grid lets the table manage the sort interaction.' },
                    },
                },
                selection: {
                    title: 'Selection modes',
                    description: 'Selection should teach one idea at a time. Single selection behaves like an active row, while multiple selection unlocks bulk commands.',
                    items: {
                        single: { tab: 'Single', title: 'Single selection with radio buttons', description: 'Single selection shows one radio per row. Choose the record from the control at the left, and click the same radio again to clear it.' },
                        multiple: { tab: 'Multiple', title: 'Multiple selection for bulk actions', description: 'Multiple selection is the right mode for bulk actions. Here the export command is a real Grid action, not a custom header button.' },
                    },
                },
                actions: {
                    title: 'Actions and editing',
                    description: 'Start from the preset when all you need is CRUD. Switch to explicit action kinds when you want to demonstrate intent, modal posture and custom workflows.',
                    items: {
                        crudPreset: { tab: 'CRUD preset', title: 'Preset add/edit/delete', description: 'This is the shortest path to add, edit and delete with the shared form.' },
                        customKinds: { tab: 'Custom kinds', title: 'Explicit modal kinds', description: 'The same CRUD surface with explicit action kinds and different modal positions for add, edit and delete.' },
                        route: { tab: 'Route', title: 'Routing action', description: 'Use a route action when the next step belongs to navigation, not to a modal workflow.' },
                    },
                },
                reorder: {
                    title: 'Drag reorder',
                    description: 'reorderable makes the drag intent explicit. Pair it with onReorder to receive the full source record set in its new order.',
                    manualOrderingTitle: 'Manual ordering',
                },
                layout: {
                    title: 'Layout surface',
                    description: 'Table and gallery keep the same Grid contract. The examples below isolate the visual switch so the layout change is the only thing to focus on.',
                    items: {
                        table: { tab: 'Table', title: 'Table layout', description: 'The default surface for lists. It uses the same records as the gallery tab so the only change is the surface.' },
                        gallery: { tab: 'Gallery', title: 'Gallery layout', description: 'The same records rendered as cards. Labels, order and data stay aligned with the table tab.' },
                    },
                },
            },
            selection: {
                chooseOneTitle: 'Choose one option',
                bulkTitle: 'Bulk selection',
                exportSelected: 'Export selected',
                exportSelectedTitle: 'Export {count} selected',
                payloadTitle: 'selection.onChange payload',
            },
            actions: {
                directoryTitle: 'Team directory',
                addTeammate: 'Add teammate',
                edit: 'Edit',
                delete: 'Delete',
                save: 'Save',
                preview: 'Preview',
                editTeammate: 'Edit teammate',
                deleteTeammateQuestion: 'Delete teammate?',
                deleteTeammateBody: 'This teammate will be removed from the mock provider:',
                deleteTeammateFromMock: 'The selected teammate will be removed from the mock provider.',
                previewTitle: 'Teammate preview',
                previewBody: 'Open a read-only side panel for the selected teammate.',
                headerDescription: 'Add opens a centered form, the custom edit footer keeps only save and delete, the base edit workflow still includes save, delete and cancel automatically, and the extra preview action docks to the right.',
                roleLabel: 'Role:',
                statusLabel: 'Status:',
                teamLabel: 'Team:',
                cityLabel: 'City:',
                goToCreatePage: 'Go to create page',
                routeActionTitle: 'Route action',
            },
            playground: {
                titleDefault: 'Team members',
                rowClickPayload: 'onRowClick payload',
                reorderPayload: 'onReorder payload',
                reorderPreviewNotice: 'Reorder preview active: the playground automatically uses view="table", source="array" and sortable=false to show drag and drop.',
                props: {
                    source: { description: 'Switch between GridDB (provider-backed, supports where/order) and GridArray (caller-owned records).' },
                    path: { description: 'GridDB: DataProvider collection path (fixed to mock in playground).' },
                    fromUrl: { description: 'GridDB: derive the path from the current route instead.' },
                    records: { description: 'GridArray: caller-owned record set. Edit the mock database below to change the data.' },
                    recordId: {
                        description: 'Single record identity strategy used across selection, reorder, edit state and create/update paths. Both _key and id are present in the playground data.',
                        help: 'Use a field name like `_key` or `id`, or a one-line arrow function. New records are saved using this same resolver, so keep it unique and stable.',
                        shortcuts: {
                            _key: { label: '_key', help: 'Use the provider/native key field.' },
                            id: { label: 'id', help: 'Use the explicit id field.' },
                            fnId: { label: 'fn id', help: 'Arrow function returning the id field.' },
                            concat: { label: 'concat', help: 'Concatenate two fields into a derived key.' },
                        },
                    },
                    where: {
                        description: 'Provider-side filter (GridDB only).',
                        help: 'Keys and string values must be quoted. Example: `{"status":"active"}`.',
                        shortcuts: {
                            empty: { label: 'empty', help: 'No provider-side filter.' },
                            active: { label: 'active', help: 'Show only active teammates.' },
                            admins: { label: 'admins', help: 'Show only admin records.' },
                            teamIn: { label: 'team in', help: 'Example with nested operators.' },
                        },
                    },
                    order: {
                        description: 'Provider-side ordering (GridDB only).',
                        help: 'Keys and direction values must be quoted. Example: `{"name":"asc"}`.',
                        shortcuts: {
                            none: { label: 'none', help: 'Keep provider default order.' },
                            nameAsc: { label: 'name asc', help: 'Order by name ascending.' },
                            emailDesc: { label: 'email desc', help: 'Order by email descending.' },
                        },
                    },
                    fieldMap: {
                        description: 'Provider-side field remapping (GridDB only). Maps target field names to source field names in the provider record.',
                        help: 'Example: `{"fullName":"name"}` maps the provider `name` field to `fullName`.',
                        shortcuts: {
                            empty: { label: 'empty', help: 'No field remapping.' },
                            fullName: { label: 'fullName', help: 'Map provider name to UI fullName.' },
                            mail: { label: 'mail', help: 'Example with multiple remapped keys.' },
                        },
                    },
                    onLoad: { description: 'Provider-side normalization hook for GridDB responses before records are built (GridDB only).' },
                    columns: {
                        description: 'Column definitions as JSON. In this playground, `{}` means inferred columns using the current record shape.',
                        help: 'Available JSON-friendly fields here: key, label, sortable, className, render. Playground shorthands supported for render: `email` and `badge`.',
                        shortcuts: {
                            infer: { label: 'infer', help: 'Uses playground auto-detect based on the current record shape.' },
                            base: { label: 'base', help: 'Neutral full example with standard text columns and email render.' },
                            compact: { label: 'compact', help: 'Shorter 3-column setup.' },
                            badge: { label: 'badge', help: 'Highlights badge and email render modes.' },
                            fieldMap: { label: 'fieldMap', help: 'Works together with the fieldMap example above.' },
                            align: { label: 'align', help: 'Shows className and alignment-oriented configuration.' },
                        },
                    },
                    view: { description: 'Visual surface used by GridCore.' },
                    sortable: {
                        description: 'Enable client-side sorting or set an initial sort order via OrderConfig.',
                        help: 'Use true/false or a JSON object like `{"name":"asc"}`.',
                        shortcuts: {
                            true: { label: 'true', help: 'Enable sortable headers.' },
                            false: { label: 'false', help: 'Disable sorting.' },
                            nameAsc: { label: 'name asc', help: 'Start with a name ascending sort.' },
                        },
                    },
                    reorderable: {
                        description: 'Enable row drag reorder. In the playground, turning this on switches the preview to the array/table setup needed for drag and drop and temporarily disables sortable sorting.',
                        help: 'Use true to enable drag and drop. The callback/function part belongs to `onReorder`, not `reorderable`.',
                        shortcuts: {
                            false: { label: 'false', help: 'Disable drag and drop.' },
                            true: { label: 'true', help: 'Enable row drag and drop.' },
                        },
                    },
                    pagination: {
                        description: 'Pagination config forwarded to Table or Gallery.',
                        shortcuts: {
                            default: { label: 'default', help: 'Default showcase pagination.' },
                            compact: { label: 'compact', help: 'Smaller page size, centered controls.' },
                            sticky: { label: 'sticky', help: 'Show sticky bottom pagination.' },
                        },
                    },
                    groupBy: {
                        description: 'Group rows by a field. Works for both table and gallery layouts. Pass a field name or a JSON array for multi-level grouping.',
                        placeholder: 'e.g. role or ["role","status"]',
                        shortcuts: {
                            off: { label: 'off', help: 'No grouping.' },
                            role: { label: 'role', help: 'Group by role.' },
                            status: { label: 'status', help: 'Group by status.' },
                            team: { label: 'team', help: 'Group by team.' },
                            roleStatus: { label: 'role+status', help: 'Multi-level: role then status.' },
                        },
                    },
                    loading: { description: 'Show a loading spinner on the grid card.' },
                    sticky: { description: 'Stick the card at the top or bottom of the scroll container.' },
                    wrapperClassName: { description: 'Extra CSS class forwarded to the outer card wrapper.' },
                    before: { description: 'Optional content rendered above the table or gallery body.' },
                    after: { description: 'Optional content rendered below the table or gallery body.' },
                    title: { description: 'Card header title.' },
                    header: {
                        description: 'Header override. Use plain text or a one-line arrow function that receives the Grid header context.',
                        help: 'Examples: static text or a function using `ctx.title` and `ctx.records.length`.',
                        shortcuts: {
                            false: { label: 'false', help: 'Use the default Grid header.' },
                            fn: { label: 'fn', help: 'Function example using the header context.' },
                            text: { label: 'text', help: 'Static text header example.' },
                        },
                    },
                    footer: {
                        description: 'Footer override. Use plain text or a one-line arrow function that receives the Grid footer context.',
                        help: 'Examples: static text or a function using `ctx.records.length` or `ctx.selection.keys.length`.',
                        shortcuts: {
                            false: { label: 'false', help: 'No custom footer.' },
                            fn: { label: 'fn', help: 'Function example using the footer context.' },
                            text: { label: 'text', help: 'Static text footer example.' },
                        },
                    },
                    actions: {
                        description: 'Action catalog as JSON. Supports CRUD array shorthand and declarative modal, delete, route and external actions.',
                        help: 'JSON cannot express functions, so this playground supports the declarative subset: static strings/booleans plus kind, label, icon, size, position, allowFullscreen, to, href, header, body and footer=false.',
                        shortcuts: {
                            none: { label: 'none', help: 'No actions.' },
                            crud: { label: 'crud', help: 'Standard add/edit/delete shortcuts.' },
                            custom: { label: 'custom', help: 'Custom modal and preview actions.' },
                            links: { label: 'links', help: 'Route and external action examples.' },
                        },
                    },
                    form: { description: 'Add/edit form. Grid wraps it in Form automatically for add and edit actions.' },
                    selection: {
                        description: 'Selection mode. The playground wires onChange automatically and shows the payload below the grid when active.',
                        help: 'Use false to disable, `single`/`multiple` for the string shorthand, or an object for `defaultKeys`.',
                        shortcuts: {
                            off: { label: 'off', help: 'No row selection.' },
                            single: { label: 'single', help: 'Single selection, Grid manages state internally.' },
                            multiple: { label: 'multiple', help: 'Multiple selection, Grid manages state internally.' },
                            defaultKeys: { label: 'defaultKeys', help: 'Single with `u1` pre-selected on mount.' },
                            multiKeys: { label: 'multi+keys', help: 'Multiple with `u1` and `u5` pre-selected on mount.' },
                        },
                    },
                    onRowClick: { description: 'Called with the full record on row or card click. Enable it to inspect the payload below.' },
                    onReorder: { description: 'Receives the reordered record array and drag metadata. Handled internally by the playground when reorderable is true.' },
                    editDeepLink: { description: 'Sync edit modal to the URL hash so the workflow survives reload and can be bookmarked.' },
                    onLoadLifecycle: { description: 'Normalize or enrich records before display. In the playground this automatically adds gallery thumbnails when layout is gallery.' },
                    onSave: { description: 'Override save target path or implement custom persistence for create/update. Handled automatically by the playground.' },
                    onDelete: { description: 'Override delete target path before the provider removes the record. Handled automatically by the playground.' },
                    onComplete: { description: 'Post-action hook. Return false to keep the modal open after save or delete.' },
                    audit: { description: 'Enable form-level audit logging during modal saves.' },
                },
            },
            propsDocs: {
                categories: {
                    data: 'Data',
                    display: 'Display',
                    layout: 'Layout',
                    actions: 'Actions',
                    behavior: 'Behavior',
                    dataLifecycle: 'Data lifecycle',
                },
                items: {
                    records: { description: 'Use with GridArray when the caller already owns the full record set.' },
                    recordId: { description: 'Single record identity strategy used for selection, reorder, edit state and create/update storage paths.' },
                    path: { description: 'Use with GridDB to specify the DataProvider collection path.' },
                    fromUrl: { description: 'Use with GridDB to derive the collection path from the current route pathname. Mutually exclusive with path.' },
                    where: { description: 'Optional provider-side filtering for GridDB.' },
                    order: { description: 'Optional provider-side ordering for GridDB.' },
                    fieldMap: { description: 'Optional provider-side field remapping for GridDB. Useful when the provider record shape does not match the UI field names.' },
                    onLoad: { description: 'Normalize or enrich records before display. Runs after the provider converts raw data into the typed record array. Supports async and works on both GridDB and GridArray.' },
                    columns: { description: 'Column definitions with sortable, className and render support. The render function receives a record-bound `runAction()`.' },
                    view: { description: 'Visual surface used by GridCore.' },
                    sortable: { description: 'Enable sorting or set the initial sort order.' },
                    pagination: { description: 'Shared pagination configuration forwarded to Table or Gallery.' },
                    groupBy: { description: 'Gallery grouping separators or field names.' },
                    loading: { description: 'Show a loading spinner on the grid card. Useful while async data is being prepared before passing it to `records`.' },
                    sticky: { description: 'Stick the card to the top or bottom of the scroll container.' },
                    wrapperClassName: { description: 'Extra CSS class forwarded to the outer card wrapper. Use it for width constraints or margins.' },
                    before: { description: 'Optional content rendered above the table or gallery body, inside the grid card.' },
                    after: { description: 'Optional content rendered below the table or gallery body, inside the grid card.' },
                    title: { description: 'Title used by the default card header.' },
                    header: { description: 'Optional custom header. Use it to replace the default title row with your own layout and actions.' },
                    footer: { description: 'Optional custom footer. Useful for bulk summaries, custom pagination wrappers or extra actions.' },
                    form: { description: 'Default add/edit form surface for CRUD workflows. Grid wraps it in Form automatically when the active action is add or edit.' },
                    actions: { description: 'Action catalog. Use the array shortcut for standard CRUD, or the record form for explicit modal, route, external, inline and delete actions.' },
                    selection: { description: 'Enable row selection. Use the string shorthand when Grid only needs selection UI, or the object form for `defaultKeys` and `onChange`.' },
                    onRowClick: { description: 'Called with the original record after row or card click.' },
                    reorderable: { description: 'Turn on row drag in table mode when used together with `onReorder`.' },
                    onReorder: { description: 'Receive the reordered source records and drag metadata.' },
                    editDeepLink: { description: 'Sync the edit modal to the URL hash so it survives reload and can be bookmarked.' },
                    onSave: { description: 'Override the save target path or implement custom persistence.' },
                    onDelete: { description: 'Override the delete target path before provider removal.' },
                    onComplete: { description: 'Post-action hook used to keep or close the current workflow.' },
                    audit: { description: 'Enable form-level audit logging during modal saves.' },
                },
            },
        },
    },
});
