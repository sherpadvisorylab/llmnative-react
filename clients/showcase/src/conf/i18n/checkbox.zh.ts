import { defineLocaleMessages } from '@llmnative/react';

export default defineLocaleMessages({
    showcase: {
        checkbox: {
            page: { title: 'Checkbox', description: '单个 checkbox 字段，在选中时会把配置好的值写入 Form 记录。' },
            sections: {
                checkedValue: { title: '选中值' },
            },
        },
    },
});
