import { defineLocaleMessages } from '@llmnative/react';

export default defineLocaleMessages({
    showcase: {
        navigationButtons: {
            page: { title: 'Navigation Buttons', description: 'Back navigation and external reference helpers.' },
            sections: {
                helpers: { title: 'Navigation helpers' },
            },
        },
    },
});
