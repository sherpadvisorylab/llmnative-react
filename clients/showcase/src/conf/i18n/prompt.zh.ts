import { defineLocaleMessages } from '@llmnative/react';
export default defineLocaleMessages({ showcase: { prompt: { page: { title: 'Prompt', description: '支持 prompt 的 textarea 字段，可存储普通文本或 AI prompt 配置，并可选择基于当前表单记录执行。' }, sections: { modes: { description: 'mode 属性用于选择界面。edit（默认）用于编写 prompt 模板；run 会显示执行控件、AI 可用状态，并把结果写回同一字段。' } }, variants: { editor: { title: 'PromptEdit', description: '使用专用编辑界面编写和维护已保存的 prompt 模板。' }, live: { title: 'PromptRun', description: '针对当前激活的表单记录执行已保存的 prompt，并将生成结果写回同一字段。' }, plain: { title: 'PromptPlain', description: '当 prompt 模式被禁用时使用的普通回退界面，字段表现为普通 textarea。' } } } } });
